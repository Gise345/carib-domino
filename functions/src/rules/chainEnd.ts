/**
 * Which end of the chain a tile is placed on. Port of the C# `ChainEnd` enum,
 * expressed as a string union (idiomatic TS, and human-readable in fixtures).
 */
export type ChainEnd = 'left' | 'right';
