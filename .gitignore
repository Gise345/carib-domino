# =============================================================================
# Unity
# =============================================================================
unity/[Ll]ibrary/
unity/[Tt]emp/
unity/[Oo]bj/
unity/[Bb]uild/
unity/[Bb]uilds/
unity/[Ll]ogs/
unity/[Uu]ser[Ss]ettings/
unity/[Mm]emoryCaptures/
unity/[Rr]ecordings/

# Repo-root build output (we saved an Android APK here mid-M3.3 testing).
# Anything matching <milestone>-android/ is a sideload build dir.
m*-android/
m*-windows/

# ParrelSync local Editor clones — symlinked copies for multi-client testing.
# Local-only dev artefact; never share via VCS.
unity_clone_*/

# IL2CPP intermediate CMake/Ninja output. Regenerated on every native build.
unity/.utmp/

# Firebase External Dependency Manager: auto-generated local Maven mirror of
# Android AARs. Resolver re-creates this any time we touch Android Firebase
# dependencies — checking it in just bloats the repo with regenerable bytes.
unity/Assets/GeneratedLocalRepo/
unity/Assets/GeneratedLocalRepo.meta

# Unity scene autosave-on-crash backups. Local-only safety net.
unity/Assets/_Recovery/

# Note: do NOT ignore *.meta files under Assets/. Unity needs them tracked
# alongside their corresponding assets — losing them breaks references.

# Uncomment this line if you wish to ignore the asset store tools plugin
# unity/[Aa]ssets/AssetStoreTools*

# Autogenerated Jetbrains Rider plugin
unity/[Aa]ssets/Plugins/Editor/JetBrains*

# Photon Fusion 2 sample bundles — vendored .unitypackage files we don't
# import. Skipping them keeps the repo ~15MB smaller; they can be
# re-downloaded from the Photon SDK at any time if needed.
unity/[Aa]ssets/Photon/FusionDemos/*.unitypackage*
unity/[Aa]ssets/Photon/FusionMenu/*.unitypackage*

# Firebase Unity SDK macOS/Linux native libs for the App core — both exceed
# GitHub's 100 MB-per-file hard limit (.bundle ≈ 191 MB, .so ≈ 123 MB on
# the 13.10 SDK). We're Windows-only for current dev; the Windows .dll
# (16 MB) is kept. macOS/iOS/Linux build machines re-vendor the SDK at
# build time to pull these files in (or switch to Git LFS at that point).
unity/[Aa]ssets/Firebase/Plugins/x86_64/FirebaseCppApp-*.bundle
unity/[Aa]ssets/Firebase/Plugins/x86_64/FirebaseCppApp-*.so

# Visual Studio cache directory
.vs/

# Unity-generated solution/project files (regenerated by Unity)
unity/*.csproj
unity/*.unityproj
unity/*.sln
unity/*.suo
unity/*.tmp
unity/*.user
unity/*.userprefs
unity/*.pidb
unity/*.booproj
unity/*.svd
unity/*.pdb
unity/*.mdb
unity/*.opendb
unity/*.VC.db

# Unity3D generated meta files
unity/*.pidb.meta
unity/*.pdb.meta
unity/*.mdb.meta

# Unity3D generated file on crash reports
unity/sysinfo.txt

# Builds
*.apk
*.aab
*.unitypackage
*.app

# Crashlytics generated file
unity/crashlytics-build.properties

# Packed Addressables
unity/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*

# Temporary auto-generated Android Assets
unity/[Aa]ssets/[Ss]treamingAssets/aa.meta
unity/[Aa]ssets/[Ss]treamingAssets/aa/*

# =============================================================================
# Node.js / Cloud Functions
# =============================================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.npm
.yarn-integrity
.pnp.*

# TypeScript build output
functions/lib/
functions/dist/
*.tsbuildinfo

# Vitest / coverage
coverage/
*.lcov
.nyc_output/

# ESLint cache
.eslintcache

# =============================================================================
# Firebase
# =============================================================================
.firebase/
firebase-debug.log*
firebase-debug.*.log*
firestore-debug.log*
ui-debug.log*
database-debug.log*
pubsub-debug.log*
storage-debug.log*

# Service account keys — never commit
*-firebase-adminsdk-*.json
serviceAccountKey*.json

# Local emulator data
emulator-data/

# =============================================================================
# Environment files & secrets
# =============================================================================
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore
*.mobileprovision
google-services.json
GoogleService-Info.plist

# RevenueCat / Photon credentials
revenuecat-secret.*
photon-app-id.*

# =============================================================================
# IDE / Editor — JetBrains (Rider, IntelliJ, etc.)
# =============================================================================
.idea/
*.iml
*.ipr
*.iws
out/

# =============================================================================
# IDE / Editor — VS Code
# =============================================================================
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.history/

# =============================================================================
# IDE / Editor — Visual Studio
# =============================================================================
*.sln.docstates
*.sln.iml
[Dd]ebug/
[Rr]elease/
[Xx]64/
[Xx]86/
[Bb]in/
[Oo]bj/
[Ll]og/

# =============================================================================
# OS — macOS
# =============================================================================
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# =============================================================================
# OS — Windows
# =============================================================================
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# =============================================================================
# OS — Linux
# =============================================================================
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# =============================================================================
# Unity — generated Addressables link.xml
# =============================================================================
# Regenerated by every Addressables/player build; tracking it produces
# spurious diffs. See commit f7d1413.
unity/Assets/AddressableAssetsData/link.xml
unity/Assets/AddressableAssetsData/link.xml.meta

# Unity/JVM crash dumps
unity/hs_err_pid*.log
